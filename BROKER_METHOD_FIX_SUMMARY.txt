🚨 CRITICAL ISSUE IDENTIFIED & FIXED
═══════════════════════════════════════════════════════════════════════════════

PROBLEM DETECTED IN BOT LOGS:
───────────────────────────────────────────────────────────────────────────────

Bot attempted to liquidate 13 positions but ALL 13 FAILED with:

  ❌ ERROR: BaseBroker.place_market_order() got unexpected keyword argument 'size'

This happened on: ICP-USD, UNI-USD, DOT-USD, AVAX-USD, BTC-USD, LINK-USD, CRV-USD, 
                  DOGE-USD, ETH-USD, HBAR-USD, APT-USD, XRP-USD, AAVE-USD

ROOT CAUSE:
───────────────────────────────────────────────────────────────────────────────

I used the wrong parameter name for the broker method:

  ❌ WRONG: place_market_order(symbol=symbol, side='sell', size=balance)
  ✅ CORRECT: place_market_order(symbol=symbol, side='sell', quantity=balance, size_type='base')

The CoinbaseBroker.place_market_order() method signature is:
  def place_market_order(self, symbol: str, side: str, quantity: float, size_type: str = 'quote')

SOLUTION APPLIED:
───────────────────────────────────────────────────────────────────────────────

✅ Fixed in 4 files:
  1. bot/trading_strategy.py - Line 362-365: Exit liquidation calls
  2. liquidate_all_now.py - Line 91-95: Emergency liquidation calls
  3. enforce_8_position_cap.py - Line 89-93: Position cap enforcer calls
  4. critical_fix_liquidate_all.py - Line 114-118: Full liquidation suite calls

Changed all instances from:
  place_market_order(symbol=X, side='sell', size=Y)
To:
  place_market_order(symbol=X, side='sell', quantity=Y, size_type='base')

DEPLOYMENT STATUS:
───────────────────────────────────────────────────────────────────────────────

1. ✅ Code fixes applied to all 4 files
2. ⏳ Ready to commit and push
3. ⏳ Container redeploy will pull corrected code
4. ⏳ Next cycle: Bot will liquidate all 13 positions successfully

NEXT STEPS:
───────────────────────────────────────────────────────────────────────────────

Run: bash /workspaces/Nija/QUICK_FIX_BROKER_METHOD.sh

Or manually:
  git add -A
  git commit -m "fix: correct broker method parameters"
  git push origin main

EXPECTED RESULTS AFTER REDEPLOY:
───────────────────────────────────────────────────────────────────────────────

Instead of:
  ❌ ❌ ERROR: BaseBroker.place_market_order() got unexpected keyword argument 'size'

You should see:
  ✅ [1/13] Selling ICP-USD... ✅ SOLD ICP-USD!
  ✅ [2/13] Selling UNI-USD... ✅ SOLD UNI-USD!
  ✅ [3/13] Selling DOT-USD... ✅ SOLD DOT-USD!
  ... (all 13 positions liquidate successfully)

EMERGENCY COMMANDS (IF NEEDED):
───────────────────────────────────────────────────────────────────────────────

Run any of these to force liquidation with corrected broker calls:

  python /workspaces/Nija/liquidate_all_now.py
  python /workspaces/Nija/enforce_8_position_cap.py
  python /workspaces/Nija/critical_fix_liquidate_all.py

TIMELINE:
───────────────────────────────────────────────────────────────────────────────

Now:        Files fixed (bot/trading_strategy.py, liquidate_all_now.py, etc)
Next:       Run QUICK_FIX_BROKER_METHOD.sh to commit & push
5-10 min:   Container auto-redeploys with corrected code
10-15 min:  Next trading cycle liquidates all 13 positions successfully
15-20 min:  NIJA should stabilize with only the good positions remaining

═══════════════════════════════════════════════════════════════════════════════
