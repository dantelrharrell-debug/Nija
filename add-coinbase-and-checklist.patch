*** Begin Patch
*** Add File: deploy-checklist.md
+# Deploy checklist — NIJA trading bot
+
+This checklist documents the minimal safety steps before enabling live trading.
+
+## Required (before enabling LIVE_TRADING=1)
+- Ensure CI (GitHub Actions) passes on `main`.
+- Confirm `bot/pyproject.toml` contains `coinbase-advanced` and `poetry.lock` is updated.
+- Add production secrets (never commit to repo):
+  - `COINBASE_API_KEY`
+  - `COINBASE_API_SECRET`
+  - `COINBASE_API_SUB`
+  - `TV_WEBHOOK_SECRET`
+  - `LIVE_TRADING` (set to `0` by default; change to `1` only after full staging verification)
+- Protect `main` branch: require pull request reviews and CI checks.
+- Smoke test staging: `/health`, `/tv/webhook` signature test, and `/trade/status`.
+
+## Rollback plan
+- Keep latest working Docker image tagged (e.g. `nija:stable`).
+- If live trading misbehaves: rotate API credentials, revert `LIVE_TRADING` to `0`, and redeploy `nija:stable`.
+
+## Notes
+- Never store secrets in repo or in plaintext files.
+- Limit who can change `LIVE_TRADING` in the production environment.
+
*** End Patch
