# Core runtime requirements
Flask>=2.3.4
gunicorn>=23.0.0
PyJWT[crypto]>=2.6.0
cryptography>=40.0.0
ecdsa>=0.18.0
requests>=2.31.0
python-dotenv>=1.0.0

# Optional ASGI server (only if using FastAPI/uvicorn)
# Uncomment only if you need an ASGI server
# uvicorn[standard]>=0.22.0

# Coinbase client (PyPI)
coinbase-advanced>=0.1.0

# Optional: install the coinbase client directly from a GitHub branch
# Replace <branch> with the branch you want
# git+https://github.com/dantelrharrell-debug/Nija.git@<branch>#egg=coinbase_advanced

# Optional: editable install for local dev
# -e .
