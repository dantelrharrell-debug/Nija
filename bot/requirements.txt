# Bot-specific dependencies (runtime)
Flask>=2.3.2
requests>=2.31.0
gunicorn>=23.0.0
python-dotenv>=1.0.0
pandas>=2.1.0
numpy>=1.26.0
loguru>=0.7.0
backoff>=2.2.1

# Crypto / JWT libs for ES256 signing
PyJWT[crypto]>=2.6.0
cryptography>=40.0.0
ecdsa>=0.18.0

# Coinbase client candidates
# Use only one official client in production (coinbase or coinbase_advanced).
# If you need coinbase_advanced from GitHub, install it manually or add a git+ URL.
coinbase>=2.0.0
