# Bot-specific dependencies (runtime)
Flask>=2.3.2
requests==2.13.0
gunicorn>=23.0.0
python-dotenv>=1.0.0
pandas>=2.1.0
numpy>=1.26.0
loguru>=0.7.0
backoff>=2.2.1

# Crypto / JWT libs for ES256 signing
PyJWT[crypto]>=2.6.0
cryptography>=40.0.0
ecdsa>=0.18.0

# Coinbase client candidates (install only the client you use)
cbpro2>=1.2.0
coinbase>=2.0.0
