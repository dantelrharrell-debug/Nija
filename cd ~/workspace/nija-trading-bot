/app
 ├─ bot/
 │    ├─ bot.py              # Coinbase trading loop
 │    ├─ requirements.txt    # Bot dependencies (coinbase client, pandas, etc.)
 │    └─ start_bot.sh        # Launches bot.py
 ├─ web/
 │    ├─ app.py              # Flask API + TradingView webhook listener
 │    ├─ requirements.txt    # Web dependencies (Flask, loguru, etc.)
 │    └─ start_web.sh        # Launches Flask/Gunicorn
 ├─ config.py                # Shared configuration (keys, trading accounts, tickers)
 └─ docker-compose.yml       # Orchestrates both containers
