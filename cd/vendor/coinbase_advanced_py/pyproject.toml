[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "coinbase_advanced"
version = "0.0.0"
description = "Vendored minimal coinbase_advanced (wrapped from coinbase_advanced_py)"
readme = "README.md"
authors = [
  { name = "Vendored", email = "dev@example.com" }
]
requires-python = ">=3.11"
packages = ["coinbase_advanced"]
