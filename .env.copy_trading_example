# ============================================================================
# NIJA COPY TRADING - SMALL ACCOUNT CONFIGURATION EXAMPLE
# ============================================================================
# This is an example configuration for small accounts ($15-$50) using copy trading.
# Copy this file to .env and add your API credentials.
#
# ⚠️  DO NOT COMMIT .env TO VERSION CONTROL!
# ============================================================================

# ============================================================================
# STEP 1: ADD YOUR EXCHANGE CREDENTIALS
# ============================================================================

# COINBASE (or other exchange)
COINBASE_API_KEY=organizations/YOUR_ORG_ID/apiKeys/YOUR_KEY_ID
COINBASE_API_SECRET="-----BEGIN EC PRIVATE KEY-----
YOUR_PRIVATE_KEY_HERE
-----END EC PRIVATE KEY-----"

# ============================================================================
# STEP 2: ENABLE COPY TRADING (REQUIRED)
# ============================================================================

# Enable copy trading - users mirror master trades
# Options: MASTER_FOLLOW (enable), INDEPENDENT (disable)
COPY_TRADING_MODE=MASTER_FOLLOW

# ============================================================================
# STEP 3: ENABLE PRO MODE (RECOMMENDED FOR SMALL ACCOUNTS)
# ============================================================================

# PRO MODE enables:
#   ✅ Faster entries
#   ✅ Smaller profit targets
#   ✅ Allows scalps that beat fees slightly
#   ⚠️  More aggressive (but with safety limits)
PRO_MODE=true

# Minimum free balance reserve % (default: 15%)
# Bot will maintain at least this % of total capital as free USD
PRO_MODE_MIN_RESERVE_PCT=0.15

# ============================================================================
# STEP 4: LOWER MINIMUM BALANCE (SAFE FOR COPY TRADING)
# ============================================================================

# Lower minimums for accounts $15-$50
# The risk engine already limits drawdown, so these are safe.
MINIMUM_TRADING_BALANCE=15.0
MIN_CASH_TO_BUY=5.0

# ============================================================================
# STEP 5: DISABLE XRP (RECOMMENDED)
# ============================================================================

# XRP has high spreads and low profit potential for small accounts
# It's already disabled by default, but you can add more pairs here
DISABLED_PAIRS=XRP-USD

# ============================================================================
# OPTIONAL: POSITION MANAGEMENT
# ============================================================================

# Keep position limits conservative for small accounts
MAX_CONCURRENT_POSITIONS=3

# ============================================================================
# STANDARD BOT CONFIGURATION
# ============================================================================

LIVE_TRADING=1
RETRY_DELAY=5
MAX_RETRIES=5

# Multi-Broker Independent Trading
MULTI_BROKER_INDEPENDENT=true

# ============================================================================
# NOTES
# ============================================================================
#
# 1. User accounts are configured in JSON files in config/users/
#    - config/users/retail_kraken.json
#    - config/users/retail_coinbase.json
#    etc.
#
# 2. Copy trading automatically scales position sizes:
#    User Position = Master Position × (User Balance / Master Balance)
#
# 3. Safety features are always enabled:
#    - Stop-loss required on all trades
#    - Daily loss limits enforced
#    - Maximum position limits respected
#
# 4. For complete setup instructions, see:
#    COPY_TRADING_SETUP.md
#
# ============================================================================
