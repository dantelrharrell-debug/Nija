[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tradingview-webhook"
version = "0.1.0"
description = "TradingView webhook helper"
authors = [{name = "Dante Harrell"}]
dependencies = []
