# Operator action

Deploy the merged commit with one Render instance and a cleared build cache. Verify the v26 launcher marker appears before any `bot.bot_main` startup logs, then confirm writer authority, canonical broker readiness, hydrated capital, and live cycles independently.
