{
  "name": "Nija (Python 3.11)",
  "image": "mcr.microsoft.com/vscode/devcontainers/python:0-3.11",
  "workspaceFolder": "/workspace",
  "forwardPorts": [5050],
  "postCreateCommand": "python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt || true"
}
