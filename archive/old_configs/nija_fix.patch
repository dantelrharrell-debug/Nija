*** Begin Patch
*** Update File: src/nija_client.py
@@
-from coinbase_advanced_py import CoinbaseClient
+from coinbase_advanced_py.client import CoinbaseClient
*** End Patch
*** Begin Patch
*** Update File: src/nija_render_worker.py
@@
-from nija_client import client  # uses live Coinbase client
-# ...
-run_worker = None
+from nija_client import client, client_is_dummy, LIVE_TRADING, DRY_RUN
+# expose the Flask app object as "run_worker" for Gunicorn (gunicorn nija_render_worker:run_worker ...)
+run_worker = app
*** End Patch
*** Begin Patch
*** Add File: start.sh
+#!/usr/bin/env bash
+# start.sh - safe startup for Render
+# NOTE: Do NOT attempt to export multiline PEM variables in this script.
+# Set COINBASE_PEM (full PEM text) or individual API vars in Render dashboard env vars.
+
+set -euo pipefail
+
+# Use the PORT Render gives us; default to 10000 for local dev
+PORT=${PORT:-10000}
+export PORT
+
+# Activate venv if it exists
+if [ -f ".venv/bin/activate" ]; then
+  source .venv/bin/activate
+fi
+
+# Run gunicorn using the Flask app exposed as run_worker
+exec gunicorn nija_render_worker:run_worker --workers 1 --bind 0.0.0.0:${PORT}
+
*** End Patch
