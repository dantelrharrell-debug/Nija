*** Begin Patch
*** Update File: Dockerfile
@@
 WORKDIR /app
-
-RUN apt-get update && \
-    apt-get install -y --no-install-recommends \
-        build-essential git ca-certificates dos2unix && \
-    apt-get clean && rm -rf /var/lib/apt/lists/*
+RUN apt-get update && \
+    apt-get install -y --no-install-recommends \
+        build-essential git ca-certificates dos2unix bash && \
+    apt-get clean && rm -rf /var/lib/apt/lists/*
 
 COPY requirements.txt /app/requirements.txt
 RUN pip install --upgrade pip setuptools wheel && \
     pip install --no-cache-dir -r /app/requirements.txt
@@
 # Copy all folders in correct order
 COPY app /app/app
 COPY web /app/web
 COPY bot /app/bot
 COPY cd /app/cd
-
-ENV PYTHONPATH=/app
-
-EXPOSE 8080
-CMD ["gunicorn", "-c", "gunicorn.conf.py", "web.wsgi:application"]
+# Ensure repo root is on Python path so package imports like `web` resolve
+ENV PYTHONPATH=/app
+
+# Optional: sanity-check import during build so failures are visible early
+RUN python - <<'PY' || true
+import importlib, sys
+try:
+    importlib.import_module('web.wsgi')
+    print('OK: web.wsgi importable')
+except Exception as e:
+    print('WARN: web.wsgi import failed during build:', e, file=sys.stderr)
+    # do not fail build here intentionally; runtime will raise if critical
+PY
+
+EXPOSE 8080
+
+# Use explicit module path so Gunicorn imports the correct app
+CMD ["gunicorn", "-c", "gunicorn.conf.py", "web.wsgi:application"]
*** End Patch
